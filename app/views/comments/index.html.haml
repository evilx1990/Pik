#comments-bg
  #comments
    .container
      .row
        -unless @comments.count.zero?
          -@comments.each do |comment|
            .comment.col-12.offset-2.col-md-6.offset-md-0
              =render 'comments/comment', comment: comment
