#category-bg
  #images-list
    .container
      .row
        -@images.each do |image|
          .image.col-6.col-md-4
            =link_to(image_tag(image.picture.url, alt: 'picture', class: 'w-100'), '#')

    #paginate-images
      =paginate @images
