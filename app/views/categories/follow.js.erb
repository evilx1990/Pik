$('<%= @block %>').html('<%= j link_to(image_tag('app_images/unfollow.resized.png', alt: 'unfollow'),
                                       unfollow_category_path(id: @category.slug, block_id: params[:block_id]),
                                       class: 'follow',
                                       remote: true,
                                       method: :put,
                                       'data-toggle' => 'tooltip',
                                       title: t('categories.index.unfollow')) %>')