$('<%= @block %>').html('<%= j link_to('follow',
                                       follow_category_path(id: @category.slug, block_id: params[:block_id]),
                                       class: 'follow',
                                       remote: true,
                                       method: :put,
                                       'data-toggle' => 'tooltip',
                                       title: t('categories.index.follow')) %>')